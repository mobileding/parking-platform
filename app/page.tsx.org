// app/page.tsx (Your main host landing page)

import ContentDisplay from '@/components/ContentDisplay';

export default function HomePage() {
  return (
    <main className="container mx-auto p-4">
      
      {/* 1. Insert the Dynamic Content Component */}
      <ContentDisplay /> 
      
      {/* 2. Host Landing Page Information */}
      <section className="mt-10 text-center">
        <h1 className="text-4xl font-extrabold text-gray-900">
          Welcome to iolab.com
        </h1>
        <p className="mt-4 text-lg text-gray-600">
          The Premier Parking Service for Christian Domain Names.
        </p>
        <div className="mt-8">
            {/* These links will be built later */}
            <a href="/signup" className="text-lg text-blue-600 hover:text-blue-800 font-semibold border-b-2 border-blue-600 mx-4">
                How to Sign Up
            </a>
            <a href="/login" className="text-lg text-green-600 hover:text-green-800 font-semibold border-b-2 border-green-600 mx-4">
                User Login
            </a>
        </div>
      </section>
      
    </main>
  );
}